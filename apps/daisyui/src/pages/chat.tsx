import { useMemo, useState } from "react"
import ReactMarkdown from "react-markdown"
import remarkGfm from "remark-gfm"
import chat from "@ui-gallery/spec/mock/chat.json"
import { Icon } from "@ui-gallery/icons-react"
import { PageHeader, Avatar } from "./shared"

export function ChatPage() {
  const [active, setActive] = useState(chat.conversations[0].id)
  const [draft, setDraft] = useState("")
  const [copied, setCopied] = useState(false)
  const [newChat, setNewChat] = useState(false)
  const [sidebar, setSidebar] = useState(false)
  const conversation = useMemo(() => chat.conversations.find((item) => item.id === active), [active])
  const suggestions = chat.suggestions.length ? chat.suggestions : chat.conversations.map((item) => item.title)
  const send = () => { if (draft.trim()) setDraft("") }
  return <div className="space-y-6"><PageHeader title="AI 对话" description="与团队智能助手协作，保留完整上下文。" /><div className="grid min-h-[650px] overflow-hidden rounded-box border border-base-300 bg-base-100 lg:grid-cols-[280px_minmax(0,1fr)]"><aside className={`${sidebar ? "block" : "hidden"} border-b border-base-300 bg-base-200/40 p-4 lg:block lg:border-b-0 lg:border-r`}><div className="flex items-center justify-between"><h2 className="font-semibold">对话</h2><button className="btn btn-ghost btn-square btn-sm" onClick={() => setNewChat(true)}><Icon name="plus" /></button></div><label className="input input-sm mt-4 w-full"><Icon name="search" size={15} /><input placeholder="搜索对话" /></label><button className="btn btn-primary btn-sm mt-3 w-full" onClick={() => setNewChat(true)}><Icon name="plus" size={15} />新建对话</button><ul className="menu mt-4 w-full gap-1 p-0">{chat.conversations.map((item) => <li key={item.id}><button className={active === item.id ? "menu-active h-auto py-3 text-left" : "h-auto py-3 text-left"} onClick={() => { setActive(item.id); setNewChat(false); setSidebar(false) }}><span className="min-w-0 flex-1"><span className="block truncate">{item.title}</span><small className="block text-base-content/60">{item.time}</small></span>{item.unread ? <span className="badge badge-primary badge-sm">{item.unread}</span> : null}</button></li>)}</ul></aside><section className="flex min-w-0 flex-col"><header className="flex items-center gap-3 border-b border-base-300 px-4 py-3"><button className="btn btn-ghost btn-square btn-sm lg:hidden" onClick={() => setSidebar(true)}><Icon name="menu" /></button><div className="min-w-0 flex-1"><p className="truncate font-medium">{newChat ? "新建对话" : conversation?.title}</p><p className="text-xs text-base-content/60">GPT-5 · 已连接</p></div><select className="select select-sm w-32"><option>gpt-5</option>{chat.models.slice(1).map((model) => <option key={model}>{model}</option>)}</select></header>{newChat ? <div className="hero flex-1"><div className="hero-content w-full text-center"><div><Icon name="sparkles" size={40} className="mx-auto" /><h2 className="mt-4 text-2xl font-bold">开始新的对话</h2><p className="mt-2 text-base-content/60">选择一个建议，或在下方输入你的问题。</p><div className="mt-5 grid gap-2 sm:grid-cols-2">{suggestions.slice(0, 4).map((item) => <button className="btn btn-outline" key={item} onClick={() => { setDraft(item); setNewChat(false) }}>{item}</button>)}</div></div></div></div> : <><div className="min-h-0 flex-1 space-y-6 overflow-auto p-4">{chat.messages.map((message, index) => <div className={`chat min-w-0 ${message.role === "user" ? "chat-end" : "chat-start"}`} key={`${message.role}-${index}`}><div className="chat-image shrink-0"><Avatar name={message.role === "user" ? "林晓" : "AI"} /></div><div className="chat-header">{message.role === "user" ? "林晓" : "AI 助手"} <time className="text-xs text-base-content/80">刚刚</time></div><div className={`chat-bubble ${message.role === "user" ? "chat-bubble-primary" : "min-w-0 w-fit max-w-[85%] overflow-hidden"}`}>{message.role === "assistant" ? <ReactMarkdown remarkPlugins={[remarkGfm]} components={{ table: ({ children }) => <div className="w-full max-w-full overflow-x-auto"><table className="table table-sm">{children}</table></div>, code: ({ children, className }) => { const text = String(children).replace(/\n$/, ""); const inline = !className; return inline ? <code>{children}</code> : <div className="mockup-code relative my-2 w-full min-w-0 max-w-full overflow-x-auto text-left"><button className="btn btn-xs absolute right-2 top-2" onClick={() => { navigator.clipboard?.writeText(text); setCopied(true) }}>{copied ? "已复制" : "复制"}</button><pre><code>{text}</code></pre></div> } }}>{message.content}</ReactMarkdown> : message.content}</div>{message.sources ? <div className="chat-footer flex flex-wrap gap-1">{message.sources.map((source) => <span className="badge badge-outline" key={source}>{source}</span>)}</div> : null}{message.tool ? <div className="collapse collapse-arrow col-start-2 mt-2 min-w-0 max-w-full border border-base-300 bg-base-200"><input type="checkbox" /><div className="collapse-title py-2 text-sm">工具调用 · {message.tool.name}</div><div className="collapse-content"><pre className="overflow-auto text-xs">{JSON.stringify(message.tool.args, null, 2)}</pre></div></div> : null}</div>)}</div><div className="border-t border-base-300 p-4"><div className="mb-3 flex flex-wrap gap-2">{suggestions.slice(0, 4).map((item) => <button className="btn btn-outline btn-sm rounded-full" key={item} onClick={() => setDraft(item)}>{item}</button>)}</div><div className="rounded-box border border-base-300 p-2"><textarea className="textarea min-h-20 w-full resize-none border-0 focus:outline-none" value={draft} onChange={(event) => setDraft(event.target.value)} placeholder="向 AI 助手提问..." /><div className="flex items-center justify-between"><button className="btn btn-ghost btn-sm"><Icon name="paperclip" size={16} />附件</button><div className="flex items-center gap-2 text-xs text-base-content/60"><kbd className="kbd kbd-sm">⌘ Enter</kbd>{draft.length} 字<button className="btn btn-primary btn-sm" onClick={send}><Icon name="send" size={16} />发送</button></div></div></div></div></>}</section></div></div>
}
