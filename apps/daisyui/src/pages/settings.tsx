import { useState } from "react"
import sessions from "@ui-gallery/spec/mock/sessions.json"
import team from "@ui-gallery/spec/mock/team.json"
import plans from "@ui-gallery/spec/mock/plans.json"
import invoices from "@ui-gallery/spec/mock/invoices.json"
import { Icon } from "@ui-gallery/icons-react"
import { PageHeader, SectionCard, StatusBadge, Avatar } from "./shared"

const tabs = ["profile", "security", "notifications", "team", "billing"] as const
const labels: Record<(typeof tabs)[number], string> = { profile: "个人资料", security: "账号安全", notifications: "通知", team: "团队", billing: "计费" }

export function SettingsPage() {
  const [tab, setTab] = useState<(typeof tabs)[number]>("profile")
  const [deleteDialog, setDeleteDialog] = useState(false)
  const [confirmation, setConfirmation] = useState("")
  return (
    <div className="space-y-6"><PageHeader title="设置" description="管理你的账户、团队与订阅设置。" /><div className="grid gap-6 lg:grid-cols-[180px_minmax(0,1fr)]"><div className="overflow-x-auto lg:overflow-visible"><ul className="menu menu-horizontal w-max gap-1 rounded-box bg-base-100 p-2 lg:menu-vertical lg:w-full">{tabs.map((item) => <li key={item}><button className={`min-h-10 ${tab === item ? "menu-active" : ""}`} onClick={() => setTab(item)}>{labels[item]}</button></li>)}</ul></div><div className="min-w-0">{tab === "profile" ? <SectionCard title="个人资料" description="更新你的公开账户信息。"><div className="space-y-6"><div className="flex flex-wrap items-center gap-4"><Avatar name="林晓" className="avatar-lg" /><div><p className="font-medium">林晓</p><p className="text-sm text-base-content/60">admin@acme.dev</p></div><input type="file" className="file-input ml-auto w-full sm:w-auto" /></div><div className="grid gap-4 sm:grid-cols-2"><fieldset className="fieldset"><legend className="fieldset-legend">姓名</legend><input className="input input-bordered w-full" defaultValue="林晓" /></fieldset><fieldset className="fieldset"><legend className="fieldset-legend">邮箱</legend><input className="input input-bordered w-full" defaultValue="admin@acme.dev" /></fieldset><fieldset className="fieldset"><legend className="fieldset-legend">语言</legend><select className="select select-bordered w-full" defaultValue="zh"><option value="zh">简体中文</option><option value="en">English</option></select></fieldset><fieldset className="fieldset"><legend className="fieldset-legend">时区</legend><input className="input input-bordered w-full" defaultValue="Asia/Shanghai" /></fieldset><fieldset className="fieldset sm:col-span-2"><legend className="fieldset-legend">简介</legend><textarea className="textarea textarea-bordered w-full" defaultValue="产品与团队协作。" /></fieldset></div><button className="btn btn-primary">保存更改</button></div></SectionCard> : tab === "security" ? <div className="space-y-4"><SectionCard title="账号安全" description="保护你的账户与登录会话。"><div className="space-y-5"><div className="flex items-center justify-between rounded-box border border-base-300 p-4"><div><p className="font-medium">双因素认证</p><p className="text-sm text-base-content/60">登录时要求额外的验证码</p></div><input type="checkbox" className="toggle toggle-primary" defaultChecked /></div><div className="skeleton size-32" /><div className="grid gap-3 sm:grid-cols-2"><input className="input input-bordered" type="password" placeholder="当前密码" /><input className="input input-bordered" type="password" placeholder="新密码" /></div><button className="btn btn-outline">修改密码</button></div></SectionCard><SectionCard title="活跃会话"><ul className="list rounded-box border border-base-300">{sessions.map((item) => <li className="list-row flex flex-wrap items-center gap-3" key={item.device}><Icon name="globe" /><div className="list-col-grow"><div>{item.device}</div><div className="text-xs text-base-content/60">{item.location} · {item.time}</div></div>{item.current ? <span className="badge badge-success">当前会话</span> : <button className="btn btn-sm btn-outline">注销</button>}</li>)}</ul></SectionCard></div> : tab === "notifications" ? <SectionCard title="通知偏好" description="选择你希望接收的通知。"><div className="space-y-4">{["项目更新", "账单提醒", "团队活动", "产品新闻"].map((item) => <label className="flex items-center justify-between border-b border-base-300 pb-4" key={item}><span>{item}</span><input type="checkbox" className="toggle toggle-primary" defaultChecked /></label>)}<div className="join"><button className="btn join-item btn-active">邮件</button><button className="btn join-item">推送</button><button className="btn join-item">站内</button></div></div></SectionCard> : tab === "team" ? <SectionCard title="团队成员" description="管理成员与访问权限。"><div className="overflow-x-auto"><table className="table table-zebra"><thead><tr><th>成员</th><th>角色</th><th>最近活跃</th><th /></tr></thead><tbody>{team.map((member) => <tr key={member.email}><td><div className="flex items-center gap-2"><Avatar name={member.name} />{member.name}</div><small className="text-base-content/60">{member.email}</small></td><td><select className="select select-sm" defaultValue={member.role}><option>owner</option><option>admin</option><option>member</option><option>viewer</option></select></td><td>{member.lastActive}</td><td><button className="btn btn-sm btn-outline btn-error">移除</button></td></tr>)}</tbody></table></div><div className="join mt-5 w-full sm:w-auto"><input className="input input-bordered join-item w-full" placeholder="成员邮箱" /><button className="btn btn-primary join-item">邀请</button></div></SectionCard> : <div className="space-y-4"><SectionCard title="当前计划" description="Pro 计划 · ¥99 / 月"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-2xl font-bold">Pro</p><p className="text-sm text-base-content/60">下次续费：2026-10-01</p></div><span className="badge badge-primary">当前计划</span></div></SectionCard><div className="grid gap-4 md:grid-cols-3">{plans.map((plan) => <div className={`card border bg-base-100 ${plan.recommended ? "border-primary" : "border-base-300"}`} key={plan.name}><div className="card-body"><div className="flex items-center justify-between"><h3 className="card-title">{plan.name}</h3>{plan.recommended ? <span className="badge badge-primary">推荐</span> : null}</div><p className="text-2xl font-bold">{plan.price === null ? "定制" : plan.price === 0 ? "免费" : `¥${plan.price}`}</p><ul className="space-y-2 text-sm">{plan.features.map((feature) => <li key={feature}><Icon name="check" className="mr-2 inline text-success" size={14} />{feature}</li>)}</ul><button className="btn btn-outline mt-2">选择方案</button></div></div>)}</div><SectionCard title="发票"><div className="overflow-x-auto"><table className="table"><thead><tr><th>编号</th><th>日期</th><th>金额</th><th>状态</th></tr></thead><tbody>{invoices.map((invoice) => <tr key={invoice.id}><td>{invoice.id}</td><td>{invoice.date}</td><td>¥{invoice.amount}</td><td><StatusBadge value={invoice.status} /></td></tr>)}</tbody></table></div></SectionCard></div>}</div></div><div className="card border border-error bg-base-100"><div className="card-body flex-row flex-wrap items-center justify-between gap-4"><div><h2 className="card-title"><span className="status status-error" aria-hidden="true" />危险区</h2><p className="text-sm text-base-content/60">删除账号后所有数据将无法恢复。</p></div><button className="btn btn-error" onClick={() => setDeleteDialog(true)}>删除账号</button></div></div>{deleteDialog ? <dialog open className="modal"><div className="modal-box"><h3 className="text-lg font-bold">删除账号</h3><p className="py-3 text-sm">请输入 DELETE 以确认。</p><input className="input input-bordered w-full" value={confirmation} onChange={(event) => setConfirmation(event.target.value)} placeholder="DELETE" /><div className="modal-action"><button className="btn" onClick={() => setDeleteDialog(false)}>取消</button><button className="btn btn-error" disabled={confirmation !== "DELETE"} onClick={() => setDeleteDialog(false)}>确认删除</button></div></div></dialog> : null}</div>
  )
}
